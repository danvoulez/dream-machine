# Lab Genesis

Fresh Lab v0 implements the root law: everything may register; only complete records matching registered process contracts activate.

Organs: memory (`logline_acts`), hands (`lab` CLI), law (`processes/`), nervous tree (`runtime/`), leaves (`runtime/adapters/`), eyes (`projections/`), imagination (`dream-machine/`), body (`fleet/`).
